# DEPRECATED: Historical prototype configuration. Do not deploy this repository.
# Production lives in Lindsay-Brunner-Website at /ai-chat-exporter/.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "npm run dev"
  targetPort = 5173
  publish = "dist"
  framework = "vite"
